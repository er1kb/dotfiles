return {
	settings = {

    python = {
      analysis = {
        typeCheckingMode = "basic", -- type checking mode can be off / basic (defaultl) / strict
        --autosearchpaths = true,
        --diagnosticmode = "workspace",
        --uselibrarycodefortypes = true

      }
    }
	},
}
